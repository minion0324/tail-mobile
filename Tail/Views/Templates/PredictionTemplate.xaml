<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:Common="clr-namespace:Tail.Common"  xmlns:controls="clr-namespace:Tail.Controls;assembly=Tail" x:Class="Tail.Views.Templates.PredictionTemplate">
     <Grid VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" RowSpacing="3" ColumnSpacing="0" HeightRequest="70">
                <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
            </Grid.ColumnDefinitions>
            <Image  HorizontalOptions="Center"
                            Source="{Binding SportImage}"
                            VerticalOptions="Center" Grid.Column="1" Grid.Row="0" />
            <Label   Style="{StaticResource WhiteLabelMedium13}" Text="{Binding PredictionPercentage}" Grid.Column="1" Grid.Row="1" HorizontalOptions="Center" VerticalOptions="Center"></Label>
                 
            </Grid>
 </ViewCell>